# AGENTS.md

本文件是本仓库中参与开发的编码 Agent 的工作规范。它约束的是“修改仓库代码的 Agent”，不是运行时负责解析表单的产品 Agent；运行时安全规则必须同时在程序和工具权限中实现，不能只依赖本文档。

## 权威资料

发生冲突时，按以下顺序处理：

1. 用户当前明确的要求；
2. `docs/product/`、`docs/architecture/` 和 `docs/discussions/` 中的项目文档；
3. `specs/` 中当前 Feature 的规格和任务；
4. `.specify/memory/constitution.md`；
5. 本文件及各模块下的 `AGENTS.md`；
6. 代码中的既有实现。

产品级变更先更新产品基线和 Roadmap，再修改代码或 Feature 规格。

## 开发流程

- 一个可独立验收的 Feature 使用一个独立分支，分支名建议为 `feat/F###-short-name`。
- 不直接向 `main` 提交；Feature 分支完成后通过 GitHub Pull Request（MR）合并。
- 开发前确认该 Feature 已有 `spec.md`、`plan.md` 和 `tasks.md`；不把整个项目写成一个巨型 Feature。
- 在有条件时为 Feature 使用独立 worktree，避免未完成改动互相污染。
- 默认以 Phase 或完整用户故事作为提交单位：Phase 内的多个 Task 可以在工作区连续完成，待该 Phase 的实现、测试和审查通过后统一提交一次；Task 不要求单独提交。若某个中间节点需要独立回滚或审查，可以额外提交，但不要为了凑提交次数拆分历史。
- 不要提交无法运行或无法解释的半成品。
- 提交前运行与改动相关的测试、类型检查和格式检查，并在 Pull Request 中记录结果。
- 不覆盖、删除或重置用户已有的无关改动。

## 产品架构边界

- 浏览器扩展只负责当前页面的读取、可见结果展示、受控填写和撤销。
- 本地 Agent 服务负责资料解析、标准化、规则校验、模型调用和当前任务编排。
- 共享数据格式以 `packages/contracts/` 中的契约为准；扩展和本地服务不得各自定义一套字段含义。
- UI 不直接调用模型；模型调用必须经过本地服务的 Provider、Policy 和 Validation 层。
- 规则优先，Agent 只处理需要语义理解的部分；模型不可用时，简单字段仍应可用。

## 运行时安全要求

- 永远不自动提交申请、发送信息、点击最终确认或绕过验证码、OTP、签名、支付和反爬机制。
- 不确定字段不能静默填写；必须返回候选、置信度和说明，或交给人工处理。
- 网页已有值默认不覆盖；冲突时展示原值和建议值并等待确认。
- 长文本生成、敏感字段填写和新增永久字段必须得到用户明确确认。
- 远程模型调用默认关闭或需用户同意，只发送完成当前任务所需的最少资料。
- 资料、API Key、用户纠正和网站配置不得写入 Git，不得出现在普通日志中。
- Agent 的输出必须经过结构化契约、策略检查和格式校验后才能交给扩展执行。
- 所有页面填写动作都应具备可追踪结果和整次撤销能力。

## 资料和适配规则

- 系统预置字段是稳定的语义标识，不等于每个网站或用户都必须填写。
- 用户自定义字段可以通过明确的确认流程创建；Agent 不得静默修改字段定义。
- 用户确认后的映射是普通的本地配置，不视为 Agent 的长期记忆或自主学习。
- 用户个人资料、申请答案、网站配置和测试样例要分开保存；测试样例必须脱敏。

## 代码质量

- 先写或补充能表达验收条件的测试，再实现行为变化。
- 安全策略、数据边界和跨模块契约优先于局部便利。
- 复杂决策写入 ADR 或对应 Feature 文档，不依赖聊天记录。
- 不为尚未进入当前 Feature 的高级功能提前引入复杂基础设施。

## 完成定义

一个 Feature 只有在以下条件全部满足时才可申请合并：

- `tasks.md` 中的任务完成或有明确说明；
- 相关测试和质量检查通过；
- 产品验收标准已验证；
- 用户可见行为、权限和失败处理已记录；
- 文档、示例和配置没有与实现脱节；
- Pull Request 获得检查和审阅后再合并。
