"""Reusable, synthetic pytest fixtures for local-agent tests.

Keeping this directory an explicit package avoids ambiguous namespace-module
resolution when static type checkers inspect both ``tests`` and its fixtures.
"""
