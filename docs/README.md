# 项目文档索引

- `product/`：产品 Brief、范围、Roadmap 和验收基线；
- `architecture/`：整体架构、模块职责和开发流程；
- `adr/`：重要架构和技术决策记录；
- `discussions/`：头脑风暴和历史讨论记录；
- `../specs/`：每个 Feature 的 SDD 规格、计划和任务。

真正的 Feature 规格不放在 `docs/` 下，统一放在仓库根目录的 `specs/`。
