# 安全与隐私

本项目处理个人简历、证件和求职申请信息。默认采用本地优先策略。

- 不要把真实个人资料、API Key、登录凭据或网站数据提交到 Git；
- 远程模型调用必须由用户明确同意，并遵循最小数据发送原则；
- 不自动提交申请，不绕过验证码、OTP、签名、支付或反爬机制；
- 发现安全问题时，不要在公开 Issue 中发布敏感细节；请通过私下渠道联系项目维护者。

详细运行时约束见 `AGENTS.md` 和 `.specify/memory/constitution.md`。
